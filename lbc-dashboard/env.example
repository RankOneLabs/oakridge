# lbc-dashboard reads these from the backend process environment.
# For local development, put real values in lbc-dashboard/.env.

# Provider keys inherited by legit-biz-club runs launched from the dashboard.
ANTHROPIC_API_KEY=your-anthropic-api-key
OPENAI_API_KEY=your-openai-api-key
OPENROUTER_API_KEY=your-openrouter-api-key

# Dashboard server config.
LBC_DASHBOARD_PORT=8765
LBC_DASHBOARD_HOST=127.0.0.1

# Control token — required when LBC_DASHBOARD_HOST is a non-loopback address
# (e.g., 0.0.0.0 or a Tailscale IP). Protects all write routes (POST/DELETE).
# Generate with: openssl rand -hex 32
# Leave unset for local loopback-only development (127.0.0.1).
#OAKRIDGE_CONTROL_TOKEN=

# Explicit insecure escape hatch — set to "1" ONLY for development when you
# deliberately want non-loopback access without a token. Do not set in production.
#ALLOW_INSECURE_NON_LOOPBACK_CONTROL=1

# Local storage paths. Defaults work for the standard oakridge layout.
LBC_RUN_ROOT=../legit-biz-club/.run
LBC_DASHBOARD_DATA_ROOT=./data
